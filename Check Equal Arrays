class Solution {
  public:
    // Function to check if two arrays are equal or not.
    bool checkEqual(vector<int>& a, vector<int>& b) {
        sort(a.begin(),a.end());
        sort(b.begin(),b.end());
        for(int i=0;i<a.size();i++)
        {
            if(a[i]!=b[i])
                return false;
        }
        return true;
    }
};


To check if two arrays are equal, compare each element of both arrays. If any element is different, return false. If all elements are the same, return true.
