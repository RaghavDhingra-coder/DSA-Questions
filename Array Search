class Solution {
  public:
    // Function to search x in arr
    // arr: input array
    // X: element to be searched for
    int search(vector<int>& arr, int x) {
        for(int i=0;i<arr.size();i++)
        {
            if(arr[i]==x)
                return i;
        }
        return -1;
    }
};

in this questions we want to search a given element in an array. here we have to just traverse in whole array and compare each array element with the given element if found return index if 
not found return -1
