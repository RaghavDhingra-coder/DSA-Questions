class Solution {
  public:
    void rotate(vector<int> &arr) {
        if(arr.size()!=1)
        {
        reverse(arr.begin(),arr.end());
        reverse(arr.begin()+1,arr.end());
        }
        
    }
};
